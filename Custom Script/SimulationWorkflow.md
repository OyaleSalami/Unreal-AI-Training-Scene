* Map manager\[MM] sets the parameters of every thing (Passed parameters)
* MM positions the camera on the selected building and sets the height
* MM sets the output resolution and directory for the movie sequence
* MM calls the sequencer to start the process
* The sequencer calls MM functions every tick(Bounding Box Calculation, Pixel Data calculation)
* The sequencer saves the file to the output and closes the application when done



